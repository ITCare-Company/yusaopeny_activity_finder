# P5 — Legacy-config fallback (no silent backend flip)

Resolve block's backend from the existing global config when no per-block value is set.

## Tasks

- [shipped (PR #4)] [legacy-global-backend-fallback](legacy-global-backend-fallback.md) — Legacy global config fallback implementation
