# W5 — Decisions

| # | Decision | Why | Source |
|---|---|---|---|
| _ex_ | _(none yet)_ | | |

Candidate decisions this wave may surface:

- `formatPlural` argument-order handling when converting filter → method (P0).
- Mixin replacement mechanism: global properties vs composable (P1).
- `v-model` model-prop strategy: rename to `modelValue`/`update:modelValue` vs
  keep `value`/`input` at call sites (P2).
- Any FontAwesome v3 API difference that touched registration (P4).
- eslint rule deviations from `vue3-recommended` the owner agreed to (P5).
