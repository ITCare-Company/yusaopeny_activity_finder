# W0 — Decisions

Append one entry per decision. Format: date · decision · why · source.

| # | Decision | Why | Source |
|---|---|---|---|
| _ex_ | _(none yet)_ | | |

Candidate decisions this wave may surface:

- Whether any Drupal-side change is unavoidable (mount markup, `drupalSettings`
  keys) → if yes, scope it into W7 explicitly.
- Whether the vestigial router can be dropped (if W0-P2 finds no `$route`
  usage) → informs W3-P1.
- Any BootstrapVue widget found beyond the expected set → informs W1-P1 and
  W4 phase scoping.
